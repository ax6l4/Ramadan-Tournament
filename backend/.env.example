# انسخ هذا الملف إلى .env وعدّل القيم قبل النشر
# Copy to .env and change values before production deploy

PORT=3000
NODE_ENV=development

# غيّر هذا المفتاح في الإنتاج — مهم للأمان
JWT_SECRET=change-this-to-a-long-random-secret
JWT_EXPIRES_IN=8h

# بيانات الأدمن
ADMIN_USERNAME=Abdul Rahman
ADMIN_PASSWORD=Aa91141702

# مسار اختياري لقاعدة البيانات
# DATABASE_PATH=./../database/tournament.db

# على Render: الصق External Database URL هنا إذا ظهر ENOTFOUND مع الرابط الداخلي
# EXTERNAL_DATABASE_URL=postgresql://USER:PASSWORD@dpg-xxxxx-a.REGION-postgres.render.com/DB
